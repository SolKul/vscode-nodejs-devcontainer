# vscode-nodejs-devcontainer

simple vscode project for development node.js in docker

# Usage

1. open Command Palette in Visual Studo Code.
1. Exec `Remote-Containers: Reopen Folder in Container`
1. wait until docker-compose build